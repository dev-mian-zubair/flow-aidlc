"""Canonical instance-relative locations for generated Flow artifacts.

Single source of truth for where `flow init` scaffolds the human-facing docs.
`.flow/`, `.claude/`, and `.mcp.json` are fixed by other conventions and are not
defined here.
"""
from __future__ import annotations

WORKLOG_DIR = "docs/flow/worklog"
KNOWLEDGE_DIR = "docs/flow/knowledge"
PRODUCT_DIR = "docs/flow/product"
